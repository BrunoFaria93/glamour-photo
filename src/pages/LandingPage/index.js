import ProductsContainer from "../../components/ProductsContainer";
import "swiper/css";

const LandingPage = ({height, open}) => {
  return <ProductsContainer height={height} open={open}/>;
};

export default LandingPage;
